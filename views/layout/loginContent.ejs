<div class="container">

    <div class="col-sm-6 col-sm-offset-3">

        <h1><span class="fa fa-sign-in"></span> 로그인</h1>

        <!-- show any messages that come back with authentication -->
        <% if (message.length > 0) { %>
            <div class="alert alert-danger">
                <%= message %>
            </div>
        <% } %>

                <!-- LOGIN FORM -->
                <form method="post">
                    <div class="form-group">
                        <label>이메일</label>
                        <input type="text" class="form-control" name="email">
                    </div>
                    <div class="form-group">
                        <label>비밀번호</label>
                        <input type="password" class="form-control" name="password">
                    </div>
                    <button type="submit" class="btn btn-info btn-lg btn-block">로그인</button>
                    <a href="/users/auth/facebook" class="btn btn-primary btn-lg btn-block">Facebook</a>
                    <a href="/users/auth/kakao" class="btn btn-warning btn-lg btn-block">Kakao</a>
                </form>

                <br>

                <p>가입이 필요하세요? <a href="/users/signup">회원가입</a></p>
                <p><a href="/">처음으로</a>.</p>

    </div>

</div>
